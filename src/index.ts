export function mapIanaToWindows(ianaTimezone: string): string {
    // TODO: Implement IANA to Windows timezone mapping
    return `Mapping ${ianaTimezone} to Windows timezone (not implemented yet)`;
}

export function mapWindowsToIana(windowsTimezone: string): string[] {
    // TODO: Implement Windows to IANA timezone mapping
    return [
        `Mapping ${windowsTimezone} to IANA timezones (not implemented yet)`,
    ];
}
